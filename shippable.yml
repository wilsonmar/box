language: none

build:
  ci:
    - export IMAGE_NAME=ttrahan/box
    - ./build.sh

  post_ci:
    - ./push.sh

integrations:
  hub:
    - integrationName: "Docker Hub"
      type: docker
      branches:
        only:
          - triton
