language: none

build:
  ci:
    - echo 'CI Running'

  post_ci:
    - docker build -t="aye0aye/box:$BRANCH.$BUILD_NUMBER" .
    - docker push aye0aye/box:$BRANCH.$BUILD_NUMBER

integrations:
  hub:
    - integrationName: "shipdeploy"
      type: docker
